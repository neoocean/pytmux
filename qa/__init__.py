"""qa/ — pytmux 자율 QA 체계 (T0 · T1 착지분).

설계 SSOT 는 이슈 트래커의 `pytmux/qa-system` 문서다(이 저장소는 M2 라 산문의 원본이
트래커에 있다). 읽는 길:

    node ../issue/bin/issue.mjs doc-get pytmux/qa-system

쓰는 법은 `qa/README.md`. 이 패키지는 **제품을 import 하지 않는다**(블랙박스 원칙) —
예외는 `pytmuxlib.ipc`/`proc` 로, 그건 로직이 아니라 **엔드포인트 경로 규약**이다.
그 규약을 베껴 적으면 제품이 경로를 바꾼 날 QA 가 조용히 남의 서버를 잰다.

★ 예외가 하나 더 있다 — `qa/inventory.py` 는 저장소 텍스트를 읽는다(AST·정규식). 그것도
**로직이 아니라 인벤토리**라서다: 명령 목록을 손으로 적으면 새 명령이 생긴 날 그 목록이
조용히 낡고, 커버리지 원장이 「전부 지났다」고 말하게 된다(`pytmux/pytmux-145`).
⛔ import 가 아니라 **텍스트 읽기**인 것이 계약이다 — import 하면 표를 만든 코드가 같이
돌아 「표가 있다」와 「도는 서버가 안다」가 뒤섞인다.
"""
